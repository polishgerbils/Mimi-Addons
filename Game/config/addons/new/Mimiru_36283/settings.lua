require('common');

local settings = T{ };
settings["emphasize"] = T{ };
settings["sounds"] = "on";
settings["background_size"] = "regular";
settings["trigger_duration"] = 3;
settings["y_position"] = 100;
settings["x_position"] = 800;

return settings;
